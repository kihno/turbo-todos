<div class="mx-auto max-w-3x1 mt-24">
  <div class="mb-6">
    <h1 class="font-bold text-4x1">Turbo Todos</h1>
  </div>

  <%= render "form", todo: Todo.new %>

<%= turbo_stream_from "todos" %>
  <div id="todos" class="divide-y list-none">
    <%= render @todos %>
  </div>
</div>
